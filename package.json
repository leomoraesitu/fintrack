{
  "name": "fintrack-firebase-rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --project demo-fintrack --only firestore \"node --test test/firebase/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.24.0"
  }
}
